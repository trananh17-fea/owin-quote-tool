/**
 * Bề mặt công khai của tính năng "Tính nhôm".
 * Ngoài feature này chỉ import từ đây, không import sâu vào file bên trong.
 * Hiện chỉ App.tsx dùng, nên bề mặt chỉ có màn hình chính.
 */
export { AluminumEstimatorView } from '@/features/aluminum/AluminumEstimatorView';
