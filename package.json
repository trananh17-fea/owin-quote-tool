{
  "name": "owin-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "owin-quote-tool",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @owin/quote-engine && npm run build -w owin-quote-tool",
    "test": "npm run test -w @owin/quote-engine && npm run test -w owin-quote-tool",
    "lint": "npm run lint -w @owin/quote-engine && npm run lint -w owin-quote-tool",
    "dev": "npm run dev -w owin-quote-tool"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "typescript-eslint": "^8.59.2",
    "vitest": "^4.1.8"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
